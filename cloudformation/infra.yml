apiVersion: apps/v1
kind: Deployment
metadata:
  name: wunder-weather
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wunder-weather
  template:
    metadata:
      labels:
        app: wunder-weather
    spec:
      containers:
      - name: wunder-weather
        image: danrhjones/wunder-weather:${$CIRCLE_BUILD_NUM}
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 50%
---
apiVersion: v1
kind: Service
metadata:
  name: wunder-weather
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: wunder-weather
